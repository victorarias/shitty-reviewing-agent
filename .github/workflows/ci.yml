name: CI

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled
  push:
    branches:
      - main
      - review-dedupe-threaded

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  LOG_TOOL_CALLS: "1"

jobs:
  harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.9"
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Run harness tests
        run: bun run test:harness
  llm-snapshots:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.9"
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Run LLM snapshot tests
        env:
          LLM_SNAPSHOT_REPLAY: "1"
        run: bun test tests/llm-snapshots.test.ts
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.9"
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Run tests
        run: bun test
      - name: Typecheck
        run: bun run build

  reviewer-latest:
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false }}
    needs: [test, harness]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    continue-on-error: true
    env:
      VERTEX_AI_API_KEY: ${{ secrets.VERTEX_AI_API_KEY }}
    steps:
      - name: Check compare-baseline label (live)
        id: compare_baseline_gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          labels="$(gh api repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels --jq '.[].name')"
          if printf '%s\n' "$labels" | grep -Fxq "compare-baseline"; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Skip reviewer when compare-baseline label is present
        if: ${{ steps.compare_baseline_gate.outputs.skip == 'true' }}
        run: echo "Skipping reviewer-latest (compare-baseline label is set)."
      - uses: actions/checkout@v4
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' }}
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v1
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' && env.VERTEX_AI_API_KEY != '' }}
        with:
          bun-version: "1.3.9"
      - name: Skip reviewer when VERTEX_AI_API_KEY is missing
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' && env.VERTEX_AI_API_KEY == '' }}
        run: echo "Skipping reviewer-latest (VERTEX_AI_API_KEY not configured)."
      - name: Install dependencies (apply local patches)
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' && env.VERTEX_AI_API_KEY != '' }}
        run: bun install --frozen-lockfile
      - name: Verify pi-ai patch is active
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' && env.VERTEX_AI_API_KEY != '' }}
        run: grep -n "VERTEX_AI_API_KEY" node_modules/@mariozechner/pi-ai/dist/providers/google-vertex.js
      - name: Run reviewer from source (patched dependencies)
        if: ${{ steps.compare_baseline_gate.outputs.skip != 'true' && env.VERTEX_AI_API_KEY != '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          env \
            GITHUB_TOKEN="${GITHUB_TOKEN}" \
            INPUT_PROVIDER=google-vertex \
            INPUT_MODEL=gemini-3-flash-preview \
            INPUT_REASONING=minimal \
            INPUT_DEBUG=true \
            INPUT_EXPERIMENTAL-PR-EXPLAINER=false \
            LLM_RATE_LIMIT_MAX_WAIT_MS=3600000 \
            LLM_RATE_LIMIT_MAX_ATTEMPTS=20 \
            "INPUT_API-KEY=${VERTEX_AI_API_KEY}" \
            bun src/index.ts

  reviewer-baseline-v028:
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false }}
    needs: [test, harness]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    continue-on-error: true
    env:
      BASELINE_API_KEY: ${{ secrets.GEMINI_API_KEY != '' && secrets.GEMINI_API_KEY || secrets.VERTEX_AI_API_KEY }}
    steps:
      - name: Check compare-baseline label (live)
        id: compare_baseline_gate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          labels="$(gh api repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels --jq '.[].name')"
          if printf '%s\n' "$labels" | grep -Fxq "compare-baseline"; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Skip baseline reviewer when compare-baseline label is absent
        if: ${{ steps.compare_baseline_gate.outputs.run != 'true' }}
        run: echo "Skipping baseline reviewer (compare-baseline label is not set)."
      - uses: actions/checkout@v4
        if: ${{ steps.compare_baseline_gate.outputs.run == 'true' }}
      - name: Skip baseline reviewer when API key is missing
        if: ${{ steps.compare_baseline_gate.outputs.run == 'true' && env.BASELINE_API_KEY == '' }}
        run: echo "Skipping baseline reviewer (configure GEMINI_API_KEY or VERTEX_AI_API_KEY)."
      - name: Run baseline reviewer v0.2.8
        if: ${{ steps.compare_baseline_gate.outputs.run == 'true' && env.BASELINE_API_KEY != '' }}
        uses: victorarias/shitty-reviewing-agent@v0.2.8
        with:
          provider: ${{ secrets.GEMINI_API_KEY != '' && 'google' || 'google-vertex' }}
          api-key: ${{ env.BASELINE_API_KEY }}
          model: gemini-3-flash-preview
          reasoning: minimal
          debug: true
          experimental-pr-explainer: false
